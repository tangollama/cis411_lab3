"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var testUtils_1 = require("./testUtils");
describe('size', function () {
    it('should return the size of the heap', function () {
        var heap = new testUtils_1.TestFibonacciHeap();
        chai_1.assert.equal(heap.size(), 0);
        heap.insert(1);
        chai_1.assert.equal(heap.size(), 1);
        heap.insert(2);
        chai_1.assert.equal(heap.size(), 2);
        heap.insert(3);
        chai_1.assert.equal(heap.size(), 3);
        heap.insert(4);
        chai_1.assert.equal(heap.size(), 4);
        heap.insert(5);
        chai_1.assert.equal(heap.size(), 5);
        heap.insert(6);
        chai_1.assert.equal(heap.size(), 6);
        heap.insert(7);
        chai_1.assert.equal(heap.size(), 7);
        heap.insert(8);
        chai_1.assert.equal(heap.size(), 8);
        heap.insert(9);
        chai_1.assert.equal(heap.size(), 9);
        heap.insert(10);
        chai_1.assert.equal(heap.size(), 10);
    });
});
//# sourceMappingURL=size.test.js.map