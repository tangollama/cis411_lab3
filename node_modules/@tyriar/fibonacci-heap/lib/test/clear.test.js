"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var testUtils_1 = require("./testUtils");
describe('clear', function () {
    it('should set the heap\'s size to 0', function () {
        var heap = new testUtils_1.TestFibonacciHeap();
        heap.insert(1);
        heap.insert(2);
        heap.insert(3);
        heap.clear();
        chai_1.assert.equal(heap.size(), 0);
    });
    it('should set the heap\'s minimum node to undefined', function () {
        var heap = new testUtils_1.TestFibonacciHeap();
        heap.insert(1);
        heap.insert(2);
        heap.insert(3);
        heap.clear();
        chai_1.assert.equal(heap.findMinimum(), undefined);
    });
});
//# sourceMappingURL=clear.test.js.map